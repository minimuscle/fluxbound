{
  "name": "fluxbound",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "ig": "cd ./apps/web && bun install && cd ../server && bun install && cd ../../packages/schema && bun install",
    "dev": "nx run-many -t dev --parallel",
    "build": "nx run-many -t build",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix"
  },
  "dependencies": {},
  "devDependencies": {
    "globals": "^17.7.0",
    "nx": "22.6.1",
    "oxfmt": "^0.41.0",
    "oxlint": "^1.71.0"
  }
}
