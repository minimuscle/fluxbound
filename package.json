{
  "name": "fluxbound",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "nx run-many -t dev --parallel",
    "build": "nx run-many -t build",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix"
  },
  "dependencies": {},
  "devDependencies": {
    "globals": "^17.3.0",
    "nx": "22.6.1",
    "oxfmt": "^0.41.0",
    "oxlint": "^1.56.0"
  }
}
